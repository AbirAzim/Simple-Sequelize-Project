<%- include('../includes/head.ejs') %>

<body>
    <%- include('../includes/nav.ejs') %>
    <div class="container">
        <% if(orders.length <=0 ) {%>
        <div class="row">
            <div class="col">

            </div>

            <div class="col">
                <h1>Order Page</h1>
            </div>

            <div class="col">

            </div>
        </div>
        <% } else {  %>
        <ul>
            <%  orders.forEach( order => { %>
            <li>
                <h1><%= order.id %></h1>
                <ul>
                    <% order.products.forEach(item => { %>
                    <li><%= item.productName %> (<%= item.orderItem.quantity %>)</li>
                    <% })%>
                </ul>
            </li>
            <% })%>
        </ul>

        <% }%>
    </div>

    <%- include('../includes/end.ejs') %>